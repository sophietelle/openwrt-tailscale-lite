name: Build Lightweight Tailscale

on:
  push:
    branches:
      - develop
  repository_dispatch:
    types: [upstream_release]
  workflow_dispatch:
    inputs:
      force_version:
        description: 'Build specific version (e.g. v1.50.0). Leave empty for auto-detect.'
        required: false
        default: ''
      skip_check:
        description: 'Skip version check and force build?'
        type: boolean
        required: false
        default: false

jobs:
  init:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
      version: ${{ steps.check.outputs.version }}
      build_tags: ${{ steps.define_tags.outputs.tags }}
    steps:
      - name: Define Build Tags
        id: define_tags
        run: |
          # --- 追加タグ定義 ---
          # must (debug dropped: pure overhead on embedded devices)
          TAGS_BASE="osrouter,unixsocketidentity"
          
          # networks (netstack depends on clientmetrics)
          #TAGS_NET="dns,listenrawdisco,portmapper"
          TAGS_NET="clientmetrics,dns,listenrawdisco,netstack,portmapper,routecheck"
          
          # manage (bakedroots omitted: relies on runtime ca-bundle from Depends;
          # c2n auto-resolved as dependency of advertiseroutes)
          TAGS_MANAGE="oauthkey"
          
          # os
          TAGS_OS="iptables,linkspeed,linuxdnsfight"
          
          # optimize (gro depends on clientmetrics) (ipnbus is required for 23.05-)
          TAGS_OPT="gro,ipnbus"
          
          # features
          TAGS_FEAT="advertiseexitnode,advertiseroutes,useexitnode,useroutes"
          
          # utilities (ssh depends on clientmetrics)
          TAGS_UTIL="wakeonlan"
          
          # 全タグをカンマ区切りで結合
          ALL_TAGS="${TAGS_BASE},${TAGS_NET},${TAGS_MANAGE},${TAGS_OS},${TAGS_OPT},${TAGS_FEAT},${TAGS_UTIL}"
          
          echo "Tags defined: $ALL_TAGS"
          echo "tags=$ALL_TAGS" >> $GITHUB_OUTPUT

      - name: Check version
        id: check
        run: |
          # 手動入力がある場合はそちらを優先
          INPUT_VER="${{ inputs.force_version }}"
          SKIP_CHECK="${{ inputs.skip_check }}"
          
          # 公式の最新タグを取得
          LATEST_TAG=$(curl -sf https://api.github.com/repos/tailscale/tailscale/releases/latest | jq -r .tag_name)
          if [ -z "$LATEST_TAG" ] || [ "$LATEST_TAG" = "null" ]; then
            echo "::error::Failed to fetch upstream latest release."
            exit 1
          fi
          
          MY_LATEST=$(curl -sf https://api.github.com/repos/${{ github.repository }}/releases/latest | jq -r .tag_name || echo "v0.0.0")
          if [ "$MY_LATEST" = "null" ]; then
            MY_LATEST="v0.0.0"
          fi
          
          echo "Upstream: $LATEST_TAG"
          echo "Current: $MY_LATEST"
          echo "Input: $INPUT_VER"
          
          if [ -n "$INPUT_VER" ]; then
            echo "Manual version specified."
            echo "should_run=true" >> $GITHUB_OUTPUT
            echo "version=$INPUT_VER" >> $GITHUB_OUTPUT
          elif [ "$SKIP_CHECK" == "true" ]; then
             echo "Skipping check, forcing build of $LATEST_TAG"
             echo "should_run=true" >> $GITHUB_OUTPUT
             echo "version=$LATEST_TAG" >> $GITHUB_OUTPUT
          elif [ "$LATEST_TAG" != "$MY_LATEST" ] || [ "${{ github.event_name }}" == "repository_dispatch" ] || [ "${{ github.event_name }}" == "push" ]; then
            echo "New version, repository_dispatch, or push to develop detected."
            echo "should_run=true" >> $GITHUB_OUTPUT
            echo "version=$LATEST_TAG" >> $GITHUB_OUTPUT
          else
            echo "Already up to date."
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

  build:
    needs: init
    if: needs.init.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          # --- x86 ---
          - goarch: 386
            openwrt_archs: "i386_pentium-mmx i386_pentium4"
          
          # --- ARM64 (Raspberry Pi 4, NanoPi, etc.) ---
          - goarch: arm64
            openwrt_archs: "aarch64_cortex-a53 aarch64_cortex-a72 aarch64_cortex-a76 aarch64_generic"
          
          # --- ARMv7 (Raspberry Pi 2/3, etc.) ---
          - goarch: arm
            goarm: 7
            openwrt_archs: "arm_cortex-a5_vfpv4 arm_cortex-a7 arm_cortex-a7_neon-vfpv4 arm_cortex-a7_vfpv4 arm_cortex-a8_vfpv3 arm_cortex-a9 arm_cortex-a9_neon arm_cortex-a9_vfpv3-d16 arm_cortex-a15_neon-vfpv4"
          
          # --- ARMv6 (Raspberry Pi 1 / Zero) ---
          - goarch: arm
            goarm: 6
            openwrt_archs: "arm_arm1176jzf-s_vfp"
          
          # --- ARMv5 ---
          - goarch: arm
            goarm: 5
            openwrt_archs: "arm_arm926ej-s arm_xscale"
          
          # --- MIPS Big Endian (Atheros AR7xxx/AR9xxx, QCA9xxx) ---
          - goarch: mips
            gomips: softfloat
            openwrt_archs: "mips_24kc mips_4kec mips_mips32"
          
          # --- MIPS Little Endian (MediaTek MT76x8, Omega2, etc.) ---
          - goarch: mipsle
            gomips: softfloat
            openwrt_archs: "mipsel_24kc mipsel_24kc_24kf mipsel_74kc mipsel_mips32"
          
          # --- RISC-V ---
          - goarch: riscv64
            openwrt_archs: "riscv64_riscv64"
          
          # --- PowerPC64 ---
          - goarch: ppc64
            openwrt_archs: "powerpc64_e5500"
          
          # --- x86_64 ---
          - goarch: amd64
            openwrt_archs: "x86_64"

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v6
        with:
          path: my-repo

      - name: Checkout Tailscale
        uses: actions/checkout@v6
        with:
          repository: tailscale/tailscale
          ref: ${{ needs.init.outputs.version }}
          path: tailscale-src
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: tailscale-src/go.mod
          cache-dependency-path: tailscale-src/go.sum

      - name: Install UPX and binutils
        run: sudo apt-get install -y upx-ucl binutils

      - name: Build Lightweight Tailscale
        working-directory: tailscale-src
        env:
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
          GOMIPS: ${{ matrix.gomips }}
          CGO_ENABLED: 0
          ALL_TAGS: ${{ needs.init.outputs.build_tags }}
        run: |
          # --extra-small オプション処理内の featuretags 実行引数に各タグを追加
          sed -i "s/--add=osrouter/--add=${ALL_TAGS}/" build_dist.sh
          grep -q "${ALL_TAGS}" build_dist.sh || { echo "::error::Failed to patch build_dist.sh. '--add=osrouter' not found."; exit 1; }
          
          chmod +x ./build_dist.sh
          echo "Building tailscale.combined for $GOARCH using build_dist.sh..."
          ./build_dist.sh --extra-small --box -o ../tailscale.combined ./cmd/tailscaled

      - name: Strip non-allocating sections
        run: |
          # Go's -s -w only drops symbols + DWARF; binutils strip --strip-all also
          # removes the build id, comment, and other non-allocating ELF sections
          # that UPX can't compress. Works on any ELF arch (strip is format-driven).
          strip --strip-all tailscale.combined || echo "Warning: strip failed for ${{ matrix.goarch }}; keeping unstripped binary."

      - name: Compress binaries with UPX
        run: |
          upx --ultra-brute --lzma tailscale.combined || echo "Warning: UPX compression failed for tailscale.combined (Arch: ${{ matrix.goarch }}). Using uncompressed binary."

      - name: Package into IPK and APK
        env:
            APK_KEY: ${{ secrets.APK_PRIVATE_KEY }}
        run: |
          VERSION=${{ needs.init.outputs.version }}
          VERSION_NUM=${VERSION#v}
          
          # 出力先ディレクトリ
          mkdir -p artifacts
          
          # アーキテクチャリストをループ
          for ARCH in ${{ matrix.openwrt_archs }}; do
            echo "Packaging for $ARCH..."
            
            # ビルド用の一時ディレクトリをクリーンアップ
            rm -rf ipk_build apk_build
            mkdir -p ipk_build/data/usr/sbin
            mkdir -p ipk_build/data/etc/config
            mkdir -p ipk_build/data/etc/init.d
            mkdir -p ipk_build/data/lib/upgrade/keep.d
            mkdir -p ipk_build/control
            mkdir -p apk_build/control
            
            # --- data ---
            cp tailscale.combined ipk_build/data/usr/sbin/
            cd ipk_build/data/usr/sbin
            ln -s tailscale.combined tailscale
            ln -s tailscale.combined tailscaled
            cd ../../../..
            
            cp my-repo/openwrt/data/tailscale.config ipk_build/data/etc/config/tailscale
            cp my-repo/openwrt/data/tailscale.init ipk_build/data/etc/init.d/tailscale
            cp my-repo/openwrt/data/tailscale.keep ipk_build/data/lib/upgrade/keep.d/tailscale
            chmod +x ipk_build/data/etc/init.d/tailscale
            
            # --- .ipk ---
            echo "Building IPK for $ARCH..."
            # 1. build data.tar.gz
            cd ipk_build/data
            tar --numeric-owner --group=0 --owner=0 -czf ../data.tar.gz *
            cd ../..
            
            # 2. copy install scripts
            cp my-repo/openwrt/control/conffiles ipk_build/control/conffiles
            cp my-repo/openwrt/control/control ipk_build/control/control
            cp my-repo/openwrt/control/postinst ipk_build/control/postinst
            cp my-repo/openwrt/control/prerm ipk_build/control/prerm
            chmod 755 ipk_build/control/postinst ipk_build/control/prerm
            
            sed -i "s/VERSION_PLACEHOLDER/$VERSION_NUM/g" ipk_build/control/control
            sed -i "s/ARCH_PLACEHOLDER/$ARCH/g" ipk_build/control/control
            
            # 3. Set data size to Installed-Size
            SIZE=$(du -sb ipk_build/data | awk '{ print $1 }')
            echo "Installed-Size: $SIZE" >> ipk_build/control/control
            
            # 4. build control.tar.gz
            cd ipk_build/control
            tar --numeric-owner --group=0 --owner=0 -czf ../control.tar.gz *
            cd ../..
            
            # --- .ipk packaging (unsign) ---
            echo "2.0" > ipk_build/debian-binary
            cd ipk_build
            IPK_NAME="tailscale_${VERSION_NUM}-r1_${ARCH}.ipk"
            tar --numeric-owner --group=0 --owner=0 -czf "$IPK_NAME" debian-binary control.tar.gz data.tar.gz
            
            # .ipk を artifacts ディレクトリへ退避
            mv "$IPK_NAME" ../artifacts/
            cd ..
            
            # --- .apk ---
            echo "Building APK for $ARCH..."
            
            # 1. build data.tar.gz
            # --format=posix + --pax-option で pax ヘッダを生成（abuild-tar --hash の前提条件）
            # abuild-tar は Alpine 専用バイナリのため stdin/stdout パイプ経由で docker 内で実行
            cd ipk_build/data
            find . -mindepth 1 -print0 | LC_ALL=C sort -z \
              | tar --format=posix \
                    --pax-option=exthdr.name=%d/PaxHeaders/%f,atime:=0,ctime:=0 \
                    --numeric-owner --owner=0 --group=0 \
                    --no-recursion --null -f - -c -T - \
                    --transform='s|^\./||' \
              | docker run --rm -i alpine:latest sh -c \
                  'apk add --no-cache abuild >/dev/null 2>&1 && abuild-tar --hash' \
              | gzip -n -9 > ../../apk_build/data.tar.gz
            cd ../..
            
            # 2. make apk metadata (.PKGINFO)
            # フィールド形式は "key = value"（スペース必須）
            # datahash は data.tar.gz 生成後に計算して末尾に追記
            {
              echo "pkgname = tailscale"
              echo "pkgver = ${VERSION_NUM}-r1"
              echo "pkgdesc = Auto-generated lightweight Tailscale build for OpenWrt."
              echo "url = https://github.com/${{ github.repository }}"
              echo "builddate = $(date +%s)"
              echo "packager = GitHub Actions <github-actions[bot]@users.noreply.github.com>"
              echo "license = BSD-3-Clause"
              echo "size = $(du -sk ipk_build/data | awk '{ print $1 * 1024 }')"
              echo "arch = $ARCH"
              echo "datahash = $(sha256sum apk_build/data.tar.gz | awk '{print $1}')"
            } > apk_build/control/.PKGINFO
            
            # 3. copy install scripts
            cp my-repo/openwrt/control/.post-install apk_build/control/.post-install
            cp my-repo/openwrt/control/.post-upgrade apk_build/control/.post-upgrade
            cp my-repo/openwrt/control/.pre-deinstall apk_build/control/.pre-deinstall
            cp my-repo/openwrt/control/.pre-upgrade apk_build/control/.pre-upgrade
            chmod 755 apk_build/control/.post-* apk_build/control/.pre-*
            
            # 4. build control.tar.gz
            # abuild-tar --cut: EOF レコードを除去（署名 prepend のため）
            cd apk_build/control
            SCRIPTS=$(ls -1 .post-* .pre-* 2>/dev/null || true)
            printf "%s\0" .PKGINFO $SCRIPTS \
              | tar --format=posix \
                    --pax-option=exthdr.name=%d/PaxHeaders/%f,atime:=0,ctime:=0 \
                    --numeric-owner --owner=0 --group=0 \
                    --no-recursion --null -f - -c -T - \
              | docker run --rm -i alpine:latest sh -c \
                  'apk add --no-cache abuild >/dev/null 2>&1 && abuild-tar --cut' \
              | gzip -n -9 > ../control.tar.gz
            cd ../..
            
            # 5. sign control.tar.gz
            echo "$APK_KEY" > myurar1a-repo.rsa
            SIGN_FILE=".SIGN.RSA.myurar1a-repo.rsa.pub"
            openssl dgst -sha1 -sign myurar1a-repo.rsa -out apk_build/${SIGN_FILE} apk_build/control.tar.gz
            
            # 6. build sign.tar.gz
            # abuild-sign の実装どおり apk_tar | abuild-tar --cut | gzip を再現
            cd apk_build
            printf "%s\0" ${SIGN_FILE} \
              | tar --format=posix \
                    --pax-option=exthdr.name=%d/PaxHeaders/%f,atime:=0,ctime:=0 \
                    --numeric-owner --owner=0 --group=0 \
                    --no-recursion --null -f - -c -T - \
              | docker run --rm -i alpine:latest sh -c \
                  'apk add --no-cache abuild >/dev/null 2>&1 && abuild-tar --cut' \
              | gzip -n -9 > sign.tar.gz
            
            # --- .apk packaging (signed) ---
            APK_NAME="tailscale-${VERSION_NUM}-r1_${ARCH}.apk"
            cat sign.tar.gz control.tar.gz data.tar.gz > "$APK_NAME"
            
            # .apk を artifacts ディレクトリへ退避し、一時ファイルを削除
            rm -f ../myurar1a-repo.rsa ${SIGN_FILE} sign.tar.gz
            mv "$APK_NAME" ../artifacts/
            cd ..
          done

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: tailscale_pkgs_${{ matrix.goarch }}_${{ matrix.goarm || '0' }}
          path: |
            artifacts/*.ipk
            artifacts/*.apk

  release:
    needs: [init, build]
    if: needs.init.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.init.outputs.version }}
          name: Tailscale Lite ${{ needs.init.outputs.version }}
          body: |
            Auto-generated lightweight tailscale build for OpenWrt.
            Upstream: https://github.com/tailscale/tailscale/releases/tag/${{ needs.init.outputs.version }}
            
            **Build Tags:** `${{ needs.init.outputs.build_tags }}`
            **Build Flags:** `--extra-small --box`
            **Compression:** `strip --strip-all` + `upx --ultra-brute --lzma`
          files: |
            artifacts/*.ipk
            artifacts/*.apk
          prerelease: ${{ github.ref == 'refs/heads/develop' }}

  deploy-repo:
    needs: [init, build]
    if: needs.init.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          path: my-repo

      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Place packages into arch directories
        run: |
          mkdir -p repo-page
          cd repo-page
          
          # --- ipk/apk: ファイル配置 ---
          find ../artifacts -type f \( -name "*.ipk" -o -name "*.apk" \) | while read -r PKG; do
            echo "Processing $PKG..."
            BASENAME=$(basename "$PKG")
            
            case "$BASENAME" in
              *.ipk)
                # ファイル名からアーキテクチャ名を抽出 (例: tailscale_1.96.3-r1_x86_64.ipk -> x86_64)
                ARCH=$(echo "$BASENAME" | sed -E 's/^tailscale_[^_]+_(.+)\.ipk$/\1/')
                mkdir -p "$ARCH"
                cp "$PKG" "$ARCH"/
                ;;
              *.apk)
                # ファイル名からアーキテクチャ名を抽出 (例: tailscale-1.96.3-r1_x86_64.apk > x86_64)
                ARCH=$(echo "$BASENAME" | sed -E 's/^tailscale-[^_]+_(.+)\.apk$/\1/')
                NEW_NAME=$(echo "$BASENAME" | sed "s/_${ARCH}//")
                mkdir -p "$ARCH"
                cp "$PKG" "$ARCH/$NEW_NAME"
                ;;
              *)
                echo "::warning::Unknown package type: $BASENAME"
                ;;
            esac
          done

      - name: Generate Index and Sign
        env:
          USIGN_KEY: ${{ secrets.USIGN_PRIVATE_KEY }}
          APK_KEY: ${{ secrets.APK_PRIVATE_KEY }}
        run: |
          cd repo-page
          
          echo "$USIGN_KEY" > usign_key.sec
          echo "$APK_KEY"   > myurar1a-repo.rsa
          trap 'rm -f "$GITHUB_WORKSPACE/repo-page/usign_key.sec" "$GITHUB_WORKSPACE/repo-page/myurar1a-repo.rsa" "$GITHUB_WORKSPACE/repo-page/myurar1a-repo.rsa.pub"' EXIT
          
          # --- ipk: インデックス生成と署名 (OpenWrt rootfsコンテナを使用) ---
          for ARCH_DIR in */; do
            if [ -d "$ARCH_DIR" ] && ls "$ARCH_DIR"/*.ipk >/dev/null 2>&1; then
              cd "$ARCH_DIR"
              
              # Generate index
              echo "Generating IPK index for $ARCH_DIR..."
              echo "" > Packages
              for IPK in *.ipk; do
                tar -xzOf "$IPK" control.tar.gz | tar xzO control >> Packages
                echo "Filename: $IPK" >> Packages
                echo "Size: $(stat -c%s "$IPK")" >> Packages
                echo "SHA256sum: $(sha256sum "$IPK" | cut -d' ' -f1)" >> Packages
                echo "" >> Packages
              done
              gzip -k -f Packages
              
              # Sign ipk
              echo "Signing Packages (IPK)..."
              docker run --rm -v "$GITHUB_WORKSPACE/repo-page":/work -w "/work/$ARCH_DIR" openwrt/rootfs:x86-64-24.10.5 \
                usign -S -m Packages -s ../usign_key.sec -x Packages.sig
              rm Packages
              cd ..
            fi
          done
          
          # --- apk: インデックス生成 ---
          docker run --rm \
            -v "$GITHUB_WORKSPACE/repo-page":/work \
            -v "$GITHUB_WORKSPACE/my-repo/cert":/cert:ro \
            -w /work \
            alpine:latest sh -c "
            
            # Install abuild
            apk add --no-cache abuild
            
            # Copy pubkey
            mkdir -p /etc/apk/keys
            cp /cert/apk_key.rsa.pub /etc/apk/keys/myurar1a-repo.rsa.pub
            cp /cert/apk_key.rsa.pub /work/myurar1a-repo.rsa.pub
            
            for ARCH_DIR in */ ; do
              if [ -d \"\$ARCH_DIR\" ] && [ \"\$ARCH_DIR\" != \"cert/\" ] && ls \"\$ARCH_DIR\"/*.apk >/dev/null 2>&1; then
                cd \"\$ARCH_DIR\"
                
                # Generate index
                echo \"Generating APK index for \$ARCH_DIR...\"
                apk index -o APKINDEX.tar.gz *.apk
                abuild-sign -q -k ../myurar1a-repo.rsa APKINDEX.tar.gz
                cd ..
              fi
            done
          "
          
          # 不要な空ディレクトリがあれば削除
          for ARCH_DIR in */; do
            if [ -d "$ARCH_DIR" ]; then
              if ! ls "$ARCH_DIR"/*.ipk >/dev/null 2>&1 && ! ls "$ARCH_DIR"/*.apk >/dev/null 2>&1; then
                echo "Skipping and removing $ARCH_DIR (no packages found)"
                rm -rf "$ARCH_DIR"
              fi
            fi
          done

      - name: Deploy to GitHub Pages
        working-directory: repo-page
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 新しいGitリポジトリとして初期化
          git init
          git checkout -b gh-pages
          git add .
          
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          
          git commit -m "Update repo ${{ needs.init.outputs.version }}"
          
          # ${{ github.repository }} を使って認証トークン付きでプッシュ
          git push --force https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git gh-pages
